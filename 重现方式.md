首先打开TestMap1，console输入Memreport -full,检查发现没有TestUI这个资源，然后点击上方按钮，会打开UMGTest并执行mixin，然后自动跳转到TestMap2页面，然后输入Memreport -full发现TestUI这个资源加载在内存中，并且无法释放，使用obj refs name=UMGTest_C可以看到该umg依然有引用存在

如果在创建UMGTest时不执行mixin，那切换地图后UMGTest以及他内部的资源会正常释放

目前发现只有当UMGTestMixin中重写了Construct并且UMGTest的蓝图中没有写Construct函数的情况下，才会出现这个问题

TestUI为UMGTest中的一张图片

测试环境为windows包